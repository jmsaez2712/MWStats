<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjectAPI" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryLightColor</item>
        <item name="colorPrimaryVariant">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="materialButtonStyle">@style/buttons</item>
        <item name="cardViewStyle">@style/cardView</item>
        <item name="textInputOutlinedStyle">@style/inputs</item>
        <item name="android:fontFamily">@font/oxanium_regular</item>
    </style>

    <style name="Theme.ProjectAPI.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ProjectAPI.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ProjectAPI.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="buttons" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">30dp</item>
        <item name="backgroundColor">@color/primaryColor</item>
    </style>

    <style name="cardView" parent="CardView">
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">3dp</item>
    </style>

    <style name="inputs" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="strokeColor">@color/primaryColor</item>
    </style>
</resources>